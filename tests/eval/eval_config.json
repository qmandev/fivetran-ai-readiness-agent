{
  "_comment": "ADK 1.15+ eval config. tool_trajectory_avg_score=1.0 requires the agent to call the exact expected tools in order. response_match_score excluded — our cases have no expected final_response (ROUGE would score 0.0 against empty string and fail every case). For a HITL agent, correct tool trajectory is the meaningful correctness signal.",
  "criteria": {
    "tool_trajectory_avg_score": 1.0
  }
}
